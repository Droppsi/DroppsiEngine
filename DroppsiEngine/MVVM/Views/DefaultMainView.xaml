<UserControl x:Class="DroppsiEngine.MVVM.Views.NewProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DroppsiEngine.MVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="Transparent">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <RadioButton Content="New Project"
                         FontSize="18"
                         Foreground="White"
                         Margin="12, 12, 12, 0"
                         Style="{StaticResource MenuButtonsBig}"
                         Command="{Binding NewProjectVM}"/>
            <RadioButton Grid.Row="1"
                         Content="Open Project From Folder"
                         FontSize="18"
                         Foreground="White"
                         Margin="12, 12, 12, 0"
                         Style="{StaticResource MenuButtonsBig}"
                         Command="{Binding OpenProjectVM}"/>
        </Grid>
    </Border>
</UserControl>
